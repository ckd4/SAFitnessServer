using System.Text;

namespace LoggerService
{
    public static class Logger
    {
        public static void Post(LogType type, string message)
        {
            StringBuilder consoleOutput = new StringBuilder();
            consoleOutput.Append();
        }
    }
}